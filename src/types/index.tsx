export  type childrenProp = {
    children: JSX.Element;
};

export interface cardI {
  id: string;
  url: string;
  title: string;
  comments: string
}

export interface videoI {
    id: string;
    url: string;
    title: string;
    comments: string
}